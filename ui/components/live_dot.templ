package components

templ LiveDot() {
    <span class="relative flex size-2">
        <span class="absolute inline-flex opacity-75 h-full w-full animate-ping rounded-full bg-green-500" />
        <span class="relative inline-flex size-2 rounded-full bg-green-600" />
    </span>
}
